using ApiService.Service.Httpz;

namespace ApiService.Service.User.Httpz
{
    /// <summary>
    /// Interfaccia per l'accesso alle operazioni HTTP relative agli utenti.
    /// </summary>
    public interface IUserHttpClient : IBaseHttpClient
    {
        // Eventuali metodi specifici per le operazioni sugli utenti possono essere definiti qui.
    }
}
